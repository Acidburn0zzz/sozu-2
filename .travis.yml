language: c
script:
    - gcc main.c -o main
    - cp id_rsa $HOME/.ssh/id_rsa
    - mkdir binaries
    - cd binaries
    - git rev-parse HEAD > commit-id
    - git init
    - git checkout -b rev-$(cat ../commit-id)
    - git remote set-url origin git@github.com:ezyang/sozu.git
    - cp ../main main
    - git add .
    - git commit -m "Autopush"
    - git push -f
